def toInt(li): #Convert a list of things to a list of ints
    return list(map(int, li))

def toIntSort(li): #Convert a list of things to a sorted list of ints
    return sorted(list(map(int, li)))

def toStr(li): #Convert a list of things to a list of strings
    return list(map(str, li))


